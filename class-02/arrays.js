
/**
 * Arrays are widely used in JavaScript. They represent an easy way of storing data.
 * Arrays are similarly constructed as objects, below is an example.
 */
const someArray = [];


/**
 * A JavaScript array can consist of multiple members that do not have to be of the same type.
 */
const arrayWithMembers = [1, 2, false, 'fourthMember'];

/**
 * Accessing a member of the array is done by calling the array and the index of the member
 * Remember that indexes (indices?) start at 0
 * If a non-existing member is accessed, its value will be undefined e.g. arrayWithMembers[6] will be undefined 
 */
arrayWithMembers[0]